class Solution:
    def maxProduct(self, nums: List[int]) -> int:
        # Sort the array in descending order
        nums.sort(reverse=True)
        
        # Return the product of the two largest elements minus 1
        return (nums[0] - 1) * (nums[1] - 1)
